/**
 * LeftSidebar component stub — to be ported from explorer/static/js/components/
 * See docs/plans/static-site-migration.md Phase 3
 */
window.LeftSidebar = {
    template: `<div class="placeholder">LeftSidebar (stub — port from explorer/)</div>`,
    setup() {
        return { store: window.store };
    },
};
