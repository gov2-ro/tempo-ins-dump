/**
 * DatasetPicker component stub — to be ported from explorer/static/js/components/
 * See docs/plans/static-site-migration.md Phase 3
 */
window.DatasetPicker = {
    template: `<div class="placeholder">DatasetPicker (stub — port from explorer/)</div>`,
    setup() {
        return { store: window.store };
    },
};
